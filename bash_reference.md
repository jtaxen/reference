Start process and leave it running:

```bash
nohup <process name>&
```

nohup = no hangups

```bash
ps -e  		# List _every_ process
ps -u  		# List all processes belonging to _user_
netstat -tulpn 	# List all server ports and which process are using them
```

